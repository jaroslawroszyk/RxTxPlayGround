[package]
name = "client"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
endpoints = { path = "../framework" }
tokio = { version = "1.46.1", features = ["full"] }
